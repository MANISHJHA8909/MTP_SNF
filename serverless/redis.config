cat > redis.config <<'EOF'
# Redis configuration is now managed via:
# - Environment variables (IP_POOL_START/END in dhcp_service.yaml)
# - Kubernetes secrets (REDIS_PASSWORD)
# - Hardcoded defaults in server.go

# This file is kept only for documentation
EOF